# -*- Mode: Python -*-

import nul

Import('target_env')
nul.App(target_env, 'sigma0',
        SOURCES = [ 'sigma0.cc'],
        INCLUDE = [  ],
        LIBS    = [ 'hostvesa', 'host', 'service', 'vsys'],
        LINKSCRIPT = 'linker.ld')
# EOF

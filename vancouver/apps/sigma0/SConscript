# -*- Mode: Python -*-

Import('output_dir')
Import('target_env')

target_env.Link(output_dir + '/apps/sigma0.nova',
                [ 
                  ],
                LIBS=['runtime'],
                linkscript="linker.ld")

# EOF

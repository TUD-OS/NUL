# -*- Mode: Python -*-

import nul

Import('target_env')

nul.App(target_env, 'root4th',
       SOURCES = [ 'root4th.c',
                   'start.S',
                   ],
       LIBS = [ 'ficl', 'crun', 'longjmp' ],
       LINKSCRIPT = 'root4th.ld')


# EOF

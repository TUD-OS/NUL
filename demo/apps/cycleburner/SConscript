# -*- Mode: Python -*-

import nul

Import('target_env')

nul.App(target_env, 'cycleburner',
        SOURCES = [ 'cycleburner.cc' ],
        INCLUDE = [ 'sigma0' ],
        LIBS    = [ 'vsys', 'string', 'nova' ],
        LINKSCRIPT = 'linker.ld')

# EOF
